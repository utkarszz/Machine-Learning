# Numpy
import numpy as np
arr = np.array([1, 2, 3, 4, 5])
print(arr)
print(arr.mean(), arr.sum(), arr.std())
matrix = np.array([[1, 2], [3, 4]])
print(matrix)
print(matrix.T)  # Transpose
# arr std set karega ki kitna data mean se door hai